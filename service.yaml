kind: Service
apiVersion: v1
metadata:
  name: as-config-service
  namespace: atticspace
spec:
  selector:
    app: as-config-server
  type: NodePort
  ports:
  - protocol: TCP
    port: 8888
    targetPort: 8888
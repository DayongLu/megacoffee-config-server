spring:
  cloud:
    gateway:
      routes:
      - id: request-route
        uri: lb://trace-monitor-api
        predicates:
        - Path=/tracemonitor/request/{requestId}
        filters:
        - SetPath=/tracemonitor/request/{requestId}
      - id: trace-monitor-api-actuator
        uri: lb://trace-monitor-api
        predicates:
        - Path=/tracemonitor/actuator/{requestId}
        filters:
        - SetPath=/actuator/{requestId}
      - id: get-all-request-route
        uri: lb://trace-monitor-api
        predicates:
        - Path=/tracemonitor/request
        filters:
        - SetPath=/tracemonitor/request
        - AddResponseHeader=Access-Control-Allow-Origin, *
      - id: event-route
        uri: lb://event-inbound-processor
        predicates:
        - Path=/event-inbound/event
        filters:
        - SetPath=/event-inbound/event
      - id: get-all-request-route
        uri: lb://trace-monitor-api
        predicates:
        - Path=/cb
        filters:
        - name: Hystrix
          args:
            name: fallbackcmd
            fallbackUri: forward:/tracemonitor/request/error
        - SetPath=/tracemonitor/request/delay


hystrix.command.fallbackcmd.execution.isolation.thread.timeoutInMilliseconds: 5000
